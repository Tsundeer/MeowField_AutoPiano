# 重构验收记录

更新时间：2026-08-07

## 已验证

- 中文为默认界面语言，语言切换无需重启，并且设置可持久化。
- 浅色/深色主题、150% DPI 下主播放页、曲库页、档案页和诊断页已完成 UI Automation 检查。
- 播放内核使用独立后台线程和高精度计时；暂停/恢复、同键重叠引用计数、连续进度快照、正负链路补偿均有回归测试。
- 配置写入使用防抖 + 串行写入门；退出时同步等待最后一次持久化。
- 曲库默认仅移出索引；删除原文件必须单独点击并经过二次确认。
- 诊断包只导出脱敏日志和环境摘要，不打包设置、曲库索引或机器身份信息。
- `MainViewModel` 已拆分为主状态、队列、持久化三个 partial 文件，主文件由 653 行降至约 500 行。
- Windows x64 自包含单文件发布包可启动，ZIP 不包含非 Windows 原生库。

## 自动化结果

当前方案测试总数：38 项。

- Domain：17 项
- Application：12 项
- Infrastructure：9 项

构建要求：`dotnet build MeowField.sln --no-restore` 为 0 warning / 0 error。

## 外部环境验收

以下项目不能在当前开发机上伪造为已通过，需要在 Windows 10 22H2 干净环境或真实目标游戏上执行：

- SendInput 与窗口消息对真实目标游戏的输入兼容性。
- PianoTrans 实际可执行文件转换流程。
- NTP 网络成功、失败、代理和休眠恢复。
- Windows 10 22H2 不同 DPI/显卡组合下的启动和渲染。
