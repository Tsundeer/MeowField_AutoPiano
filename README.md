# MeowField_AutoPiano

MeowField_AutoPiano 是一款 Windows 自动弹琴软件：加载 MIDI 后绑定目标窗口即可自动演奏，也可以把普通音频文件一键转换成 MIDI 谱。软件专为《开放空间》PC 端乐器玩法适配，支持钢琴、架子鼓、麦克风等乐器的自动演奏。

当前版本：**2.2.7**（2.0 为正式版基线）  
作者 / 软件署名：**薮猫**  
项目主页：<https://github.com/Tsundeer/MeowField_AutoPiano>  
下载：GitHub [Releases](https://github.com/Tsundeer/MeowField_AutoPiano/releases)

## 功能特性

- **自动弹琴**：加载 MIDI 后自动向目标窗口发送按键，支持钢琴、架子鼓、麦克风模式
- **音频转 MIDI 谱**：将 WAV / MP3 / FLAC / OGG / M4A / AAC 音频转换为 MIDI，转换完成直接载入播放
- **在线曲库**：内置在线 MIDI 曲库，进入页面自动加载，支持搜索、分类、大小、排序组合筛选，点击即载入播放，不占用本地磁盘
- **本地曲库**：扫描目录批量收录 MIDI，支持搜索、分页浏览和曲目管理
- **播放队列**：多曲目排队、自动下一首、手动切换、拖拽进度条、暂停与继续
- **定时任务**：按本地时间或 NTP 校时，在指定时刻自动开播
- **键位与预设**：自定义 MIDI 音符到游戏键位的映射，保存游戏配置和参数预设
- **演奏参数**：速度、移调、最大复音、和弦处理、链路延迟补偿、自动移调等
- **双输入方式**：SendInput 与窗口消息两种模式，适配不同游戏窗口
- **界面与体验**：中文/英文、浅色/深色主题、虚拟键盘预览、日志与诊断导出、内置在线更新

## 安装与更新

- 支持 Windows 10 / 11 64 位系统
- 从 GitHub Releases 下载 `MeowField_AutoPiano-*-win-x64-Setup.exe` 安装即可
- 安装包为自包含版本，普通用户**不需要**安装 .NET、Python 或 Node.js
- 部分游戏窗口需要管理员权限才能稳定发送按键，建议以管理员身份运行
- 在“设置”页点击“检查更新”，软件会自动下载新版安装包并拉起安装，无需手动替换文件

## 快速上手

1. 安装并启动 MeowField_AutoPiano。
2. 打开《开放空间》PC 端，进入乐器演奏界面。
3. 在“播放”页选择目标窗口并刷新绑定。
4. 打开本地 MIDI、从在线曲库选择曲目，或直接把 `.mid` / `.midi` 文件拖入窗口。
5. 选择乐器（钢琴 / 架子鼓 / 麦克风）、输入方式和键位映射。
6. 点击播放，或使用全局快捷键开始演奏。

> 使用 SendInput 时，目标游戏窗口必须处于前台；窗口失去焦点后程序会自动停止发送，不会误操作其他程序。窗口消息模式适用于支持后台消息的游戏。

## 音频转 MIDI 谱

1. 在“音频转谱”页选择音频文件（WAV / MP3 / FLAC / OGG / M4A / AAC）。
2. 首次使用请在转换页指定本机 PianoTrans 所在目录（程序也会自动查找常见位置）。
3. 点击转换，等待 PianoTrans 完成识别。
4. 转换出的 MIDI 会自动载入播放器，可直接绑定窗口开始弹奏。

> 音频转谱需要本机安装 PianoTrans，转换耗时取决于音频长度和电脑性能。

## 在线曲库

- 进入“在线曲库”页面会自动拉取最新曲目索引
- 支持关键词搜索，以及分类、大小、排序组合筛选
- 点击“加载播放”即可把曲目载入播放器，曲目不写入本地磁盘，避免数据膨胀

## 数据与隐私

软件设置、曲库索引、播放列表和预设保存在 Windows 用户本地应用数据目录；在线曲库只读取不保存。日志可在“日志与诊断”页面导出诊断包。

## 从源码构建（开发者）

本仓库只提交源代码、测试和构建脚本，不包含 `bin/`、`obj/`、`artifacts/` 等编译产物。普通用户请直接使用 GitHub Releases 中的安装包。

```powershell
dotnet build MeowField.sln
dotnet test MeowField.sln --no-build
.\scripts\publish-win-x64.ps1
```

构建输出仅用于本地发布，不应提交到 GitHub。

## 开源许可

本项目采用 GPL-3.0 开源许可，详见 [`LICENSE`](LICENSE)。
