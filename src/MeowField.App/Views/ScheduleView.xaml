<UserControl x:Class="MeowField.App.Views.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <Grid Margin="24,20">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="20" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Style="{StaticResource HeadingText}" Text="定时任务" />
            <TextBlock Margin="0,5,0,0" Style="{StaticResource SecondaryText}" Text="使用本地时间或 NTP 偏移，在指定时刻启动当前 MIDI" />
        </StackPanel>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions><ColumnDefinition Width="1.1*" /><ColumnDefinition Width="20" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <Border Background="{DynamicResource Brush.Surface.Panel}" BorderBrush="{DynamicResource Brush.Border.Default}" BorderThickness="1" CornerRadius="6" Padding="20">
                <StackPanel>
                    <TextBlock FontWeight="SemiBold" Text="校时状态" />
                    <TextBlock Margin="0,16,0,0" Style="{StaticResource SecondaryText}" Text="服务器与测量结果" />
                    <TextBlock Margin="0,7,0,0" TextWrapping="Wrap"><TextBlock.Text><MultiBinding Converter="{StaticResource UiTextConverter}"><Binding Path="ServerText" /><Binding Path="DataContext.IsEnglish" RelativeSource="{RelativeSource AncestorType=Window}" /></MultiBinding></TextBlock.Text></TextBlock>
                    <Button Margin="0,18,0,0" HorizontalAlignment="Left" Command="{Binding SyncNtpCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}">
                        <StackPanel Orientation="Horizontal"><iconPacks:PackIconLucide Kind="RefreshCw" Width="15" Height="15" /><TextBlock Margin="7,0,0,0" Text="测量 NTP" /></StackPanel>
                    </Button>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Background="{DynamicResource Brush.Surface.Panel}" BorderBrush="{DynamicResource Brush.Border.Default}" BorderThickness="1" CornerRadius="6" Padding="20">
                <StackPanel>
                    <TextBlock FontWeight="SemiBold" Text="执行时间" />
                    <TextBlock Margin="0,16,0,6" Style="{StaticResource SecondaryText}" Text="日期" />
                    <DatePicker SelectedDate="{Binding ScheduledDate}" Height="32" />
                    <TextBlock Margin="0,14,0,6" Style="{StaticResource SecondaryText}" Text="时间" />
                    <TextBox Height="32" Padding="9,5" Text="{Binding ScheduledTime}" />
                    <TextBlock Margin="0,14,0,6" Style="{StaticResource SecondaryText}" Text="MIDI 路径（留空使用当前曲目）" />
                    <TextBox Height="32" Padding="9,5" Text="{Binding MidiPath}" />
                    <CheckBox Margin="0,16,0,0" Content="使用最近一次 NTP 偏移" IsChecked="{Binding UseNtp}" />
                    <StackPanel Margin="0,18,0,0" Orientation="Horizontal">
                        <Button Style="{StaticResource PrimaryButton}" Command="{Binding ScheduleCommand}">设置任务</Button>
                        <Button Margin="8,0,0,0" Command="{Binding CancelScheduleCommand}">取消任务</Button>
                    </StackPanel>
                    <TextBlock Margin="0,16,0,0" TextWrapping="Wrap"><TextBlock.Text><MultiBinding Converter="{StaticResource UiTextConverter}"><Binding Path="StatusText" /><Binding Path="DataContext.IsEnglish" RelativeSource="{RelativeSource AncestorType=Window}" /></MultiBinding></TextBlock.Text></TextBlock>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
